import bookingRoutes from '../../routes/bookings.js';

// Non-breaking module adapter:
// keep current API behavior while transitioning route ownership to modules/booking.
export default bookingRoutes;
