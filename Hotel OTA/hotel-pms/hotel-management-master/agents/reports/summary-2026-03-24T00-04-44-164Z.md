# Executive Summary

## Overview

The Code Reviewer Agent system analyzed the Hotel Management System (PMS) codebase.

- **Files Analyzed:** 1535
- **Agents Deployed:** 10
- **Total Findings:** 3319
- **Analysis Duration:** 1.9s

## Risk Assessment: **CRITICAL**

### Critical Issues Requiring Immediate Attention

1. **Hardcoded password detected** (backend\src\controllers\apiManagementController.js)
   Found what appears to be a hardcoded password at line 1174. Hardcoded credentials are a critical security risk — they can be exposed in version control and are difficult to rotate.

1. **Hardcoded secret/API key detected** (backend\src\routes\hotelSettings.js)
   Found what appears to be a hardcoded secret/API key at line 148. Hardcoded credentials are a critical security risk — they can be exposed in version control and are difficult to rotate.

1. **Hardcoded secret/API key detected** (backend\src\routes\hotelSettings.js)
   Found what appears to be a hardcoded secret/API key at line 151. Hardcoded credentials are a critical security risk — they can be exposed in version control and are difficult to rotate.

1. **Hardcoded secret/API key detected** (backend\src\routes\hotelSettings.js)
   Found what appears to be a hardcoded secret/API key at line 154. Hardcoded credentials are a critical security risk — they can be exposed in version control and are difficult to rotate.

1. **Hardcoded secret/API key detected** (backend\src\routes\ota.js)
   Found what appears to be a hardcoded secret/API key at line 34. Hardcoded credentials are a critical security risk — they can be exposed in version control and are difficult to rotate.

1. **Admin route without authorization check** (backend\src\routes\addOnServices.js)
   backend\src\routes\addOnServices.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\allotment.js)
   backend\src\routes\allotment.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\contact.js)
   backend\src\routes\contact.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\dayUse.js)
   backend\src\routes\dayUse.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\meetUpResources.js)
   backend\src\routes\meetUpResources.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\notifications.js)
   backend\src\routes\notifications.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\reorder.js)
   backend\src\routes\reorder.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\seasonalPricing.js)
   backend\src\routes\seasonalPricing.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\serviceTypes.js)
   backend\src\routes\serviceTypes.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\settings.js)
   backend\src\routes\settings.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\upload.js)
   backend\src\routes\upload.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Admin route without authorization check** (backend\src\routes\webOptimization.js)
   backend\src\routes\webOptimization.js appears to define admin-related routes but does not use authorization middleware. Any authenticated user could access admin functionality.

1. **Potential NoSQL injection vulnerability** (backend\src\controllers\corporateCreditController.js)
   backend\src\controllers\corporateCreditController.js appears to pass user input directly into MongoDB queries. Attackers can inject operators like $gt, $ne, $regex to bypass query logic.

1. **Potential NoSQL injection vulnerability** (backend\src\controllers\posController.js)
   backend\src\controllers\posController.js appears to pass user input directly into MongoDB queries. Attackers can inject operators like $gt, $ne, $regex to bypass query logic.

1. **Potential NoSQL injection vulnerability** (backend\src\controllers\revenueAccountController.js)
   backend\src\controllers\revenueAccountController.js appears to pass user input directly into MongoDB queries. Attackers can inject operators like $gt, $ne, $regex to bypass query logic.

1. **Potential NoSQL injection vulnerability** (backend\src\controllers\roomChargeController.js)
   backend\src\controllers\roomChargeController.js appears to pass user input directly into MongoDB queries. Attackers can inject operators like $gt, $ne, $regex to bypass query logic.

1. **Potential NoSQL injection vulnerability** (backend\src\controllers\roomTaxController.js)
   backend\src\controllers\roomTaxController.js appears to pass user input directly into MongoDB queries. Attackers can inject operators like $gt, $ne, $regex to bypass query logic.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\allotmentController.js)
   At line 783 in backend\src\controllers\allotmentController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\bankAccountController.js)
   At line 194 in backend\src\controllers\bankAccountController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\billMessageController.js)
   At line 245 in backend\src\controllers\billMessageController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\channelManagerController.js)
   At line 147 in backend\src\controllers\channelManagerController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\dayUseController.js)
   At line 205 in backend\src\controllers\dayUseController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\enhancedBookingController.js)
   At line 556 in backend\src\controllers\enhancedBookingController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\financialController.js)
   At line 169 in backend\src\controllers\financialController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\financialController.js)
   At line 354 in backend\src\controllers\financialController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\financialController.js)
   At line 384 in backend\src\controllers\financialController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\groupBookingController.js)
   At line 55 in backend\src\controllers\groupBookingController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\groupBookingController.js)
   At line 300 in backend\src\controllers\groupBookingController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\measurementUnitController.js)
   At line 258 in backend\src\controllers\measurementUnitController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\posAttributeController.js)
   At line 233 in backend\src\controllers\posAttributeController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\posController.js)
   At line 371 in backend\src\controllers\posController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\posTaxController.js)
   At line 217 in backend\src\controllers\posTaxController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\waitlistController.js)
   At line 209 in backend\src\controllers\waitlistController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\controllers\workflowController.js)
   At line 112 in backend\src\controllers\workflowController.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\controllers\workflowController.js)
   At line 663 in backend\src\controllers\workflowController.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\advancedSearchService.js)
   At line 812 in backend\src\services\advancedSearchService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\allotmentService.js)
   At line 915 in backend\src\services\allotmentService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\allotmentService.js)
   At line 1543 in backend\src\services\allotmentService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\analytics\ETLService.js)
   At line 257 in backend\src\services\analytics\ETLService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\bookingComConnector.js)
   At line 65 in backend\src\services\bookingComConnector.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\bookingComConnector.js)
   At line 55 in backend\src\services\bookingComConnector.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\bookingComConnector.js)
   At line 110 in backend\src\services\bookingComConnector.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\bookingComConnector.js)
   At line 247 in backend\src\services\bookingComConnector.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\bookingComConnector.js)
   At line 345 in backend\src\services\bookingComConnector.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\bookingFormService.js)
   At line 562 in backend\src\services\bookingFormService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\bookingFormService.js)
   At line 542 in backend\src\services\bookingFormService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channelManager.js)
   At line 146 in backend\src\services\channelManager.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channelManager.js)
   At line 243 in backend\src\services\channelManager.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channelManager.js)
   At line 304 in backend\src\services\channelManager.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channelManager.js)
   At line 317 in backend\src\services\channelManager.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channelManager.js)
   At line 547 in backend\src\services\channelManager.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channels\bookingComService.js)
   At line 481 in backend\src\services\channels\bookingComService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\channelSyncService.js)
   At line 11 in backend\src\services\channelSyncService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\creditMonitoringService.js)
   At line 898 in backend\src\services\creditMonitoringService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\dayUseService.js)
   At line 247 in backend\src\services\dayUseService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\discountPricingService.js)
   At line 38 in backend\src\services\discountPricingService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\discountPricingService.js)
   At line 103 in backend\src\services\discountPricingService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\financialService.js)
   At line 285 in backend\src\services\financialService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\financialService.js)
   At line 301 in backend\src\services\financialService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\financialService.js)
   At line 384 in backend\src\services\financialService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\financialService.js)
   At line 432 in backend\src\services\financialService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\guestManagementService.js)
   At line 37 in backend\src\services\guestManagementService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\guestManagementService.js)
   At line 79 in backend\src\services\guestManagementService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\guestManagementService.js)
   At line 175 in backend\src\services\guestManagementService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\housekeepingAutomationService.js)
   At line 648 in backend\src\services\housekeepingAutomationService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\inventoryAlertService.js)
   At line 123 in backend\src\services\inventoryAlertService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\inventoryAlertService.js)
   At line 187 in backend\src\services\inventoryAlertService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\inventoryAlertService.js)
   At line 197 in backend\src\services\inventoryAlertService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\inventoryAlertService.js)
   At line 215 in backend\src\services\inventoryAlertService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\inventoryBillingService.js)
   At line 264 in backend\src\services\inventoryBillingService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\notificationRouting.js)
   At line 116 in backend\src\services\notificationRouting.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\operationalManagementService.js)
   At line 37 in backend\src\services\operationalManagementService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\operationalManagementService.js)
   At line 79 in backend\src\services\operationalManagementService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\services\operationalManagementService.js)
   At line 107 in backend\src\services\operationalManagementService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\queueService.js)
   At line 826 in backend\src\services\queueService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\resourceSharingService.js)
   At line 363 in backend\src\services\resourceSharingService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\resourceSharingService.js)
   At line 460 in backend\src\services\resourceSharingService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\roomBookingService.js)
   At line 137 in backend\src\services\roomBookingService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\roomBookingService.js)
   At line 335 in backend\src\services\roomBookingService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\roomBookingService.js)
   At line 554 in backend\src\services\roomBookingService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\seasonalPricingService.js)
   At line 201 in backend\src\services\seasonalPricingService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\services\tapeChartService.js)
   At line 1024 in backend\src\services\tapeChartService.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\tapeChartService.js)
   At line 479 in backend\src\services\tapeChartService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\tapeChartService.js)
   At line 541 in backend\src\services\tapeChartService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\services\tapeChartService.js)
   At line 1059 in backend\src\services\tapeChartService.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\addOnServices.js)
   At line 28 in backend\src\routes\addOnServices.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\billingHistory.js)
   At line 414 in backend\src\routes\billingHistory.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\bookingConversations.js)
   At line 92 in backend\src\routes\bookingConversations.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\routes\bookings.js)
   At line 974 in backend\src\routes\bookings.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\bookings.js)
   At line 1014 in backend\src\routes\bookings.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\bookings.js)
   At line 1201 in backend\src\routes\bookings.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\channelManagement.js)
   At line 134 in backend\src\routes\channelManagement.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\channelManagement.js)
   At line 247 in backend\src\routes\channelManagement.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\routes\checkoutInventory.js)
   At line 290 in backend\src\routes\checkoutInventory.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\checkoutInventory.js)
   At line 341 in backend\src\routes\checkoutInventory.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\checkoutInventory.js)
   At line 402 in backend\src\routes\checkoutInventory.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\dailyInventoryCheck.js)
   At line 329 in backend\src\routes\dailyInventoryCheck.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\dayUse.js)
   At line 13 in backend\src\routes\dayUse.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\externalBookings.js)
   At line 21 in backend\src\routes\externalBookings.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\hotelServices.js)
   At line 358 in backend\src\routes\hotelServices.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\housekeepingAutomation.js)
   At line 530 in backend\src\routes\housekeepingAutomation.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\routes\inventory.js)
   At line 122 in backend\src\routes\inventory.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\inventoryConsumption.js)
   At line 716 in backend\src\routes\inventoryConsumption.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\ota.js)
   At line 22 in backend\src\routes\ota.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\ota.js)
   At line 184 in backend\src\routes\ota.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\payments.js)
   At line 594 in backend\src\routes\payments.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\seasonalPricing.js)
   At line 16 in backend\src\routes\seasonalPricing.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\routes\settlements.js)
   At line 448 in backend\src\routes\settlements.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\staffDashboard.js)
   At line 439 in backend\src\routes\staffDashboard.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\testCheckouts.js)
   At line 376 in backend\src\routes\testCheckouts.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Double booking vulnerability — check-then-act race condition** (backend\src\routes\waitingList.js)
   At line 133 in backend\src\routes\waitingList.js: Availability is checked and then a booking is created in separate operations without locking. Two concurrent requests for the same room/time can both pass the availability check and both create bookings (TOCTOU race condition).

1. **Read-modify-write race condition in critical operation** (backend\src\routes\webhooks.js)
   At line 140 in backend\src\routes\webhooks.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Read-modify-write race condition in critical operation** (backend\src\routes\webhooks.js)
   At line 183 in backend\src\routes\webhooks.js: A document is read, modified in JavaScript, and saved back without atomic operations or a transaction. If two requests execute concurrently, one update will be lost.

1. **Payment webhook without signature verification** (backend\src\controllers\paymentMethodController.js)
   backend\src\controllers\paymentMethodController.js handles payment webhooks but may not verify the webhook signature. Without verification, attackers can send fake payment confirmations to mark unpaid bookings as paid.

1. **Payment webhook without signature verification** (backend\src\services\otaPayloadService.js)
   backend\src\services\otaPayloadService.js handles payment webhooks but may not verify the webhook signature. Without verification, attackers can send fake payment confirmations to mark unpaid bookings as paid.

1. **Payment webhook without signature verification** (backend\src\services\payloadRetentionService.js)
   backend\src\services\payloadRetentionService.js handles payment webhooks but may not verify the webhook signature. Without verification, attackers can send fake payment confirmations to mark unpaid bookings as paid.

1. **Payment webhook without signature verification** (backend\src\models\OTAPayload.js)
   backend\src\models\OTAPayload.js handles payment webhooks but may not verify the webhook signature. Without verification, attackers can send fake payment confirmations to mark unpaid bookings as paid.

## Agent Results

| Agent | Findings | Duration |
|-------|----------|----------|
| CodebaseAnalyzer | 359 | 702ms |
| DataFlowAgent | 72 | 466ms |
| BugDetectionAgent | 1226 | 252ms |
| SecurityAuditAgent | 73 | 191ms |
| PerformanceAgent | 944 | 103ms |
| ArchitectureAgent | 219 | 25ms |
| ConcurrencyAgent | 225 | 108ms |
| BookingSystemAgent | 156 | 43ms |
| PaymentFlowAgent | 45 | 16ms |
| RefactorExecutionAgent | 0 | 7ms |

## Top Recommendations

1. **Potentially undefined route handler: getDepartmentById** [HIGH]
   → Verify that "getDepartmentById" is properly imported or defined in this file.

2. **Potentially undefined route handler: logReasonUsage** [HIGH]
   → Verify that "logReasonUsage" is properly imported or defined in this file.

3. **Route file missing authentication: testCheckouts.js** [HIGH]
   → Add authentication middleware (e.g., authenticate) to all state-changing routes.

4. **Route file missing authentication: webhooks.js** [HIGH]
   → Add authentication middleware (e.g., authenticate) to all state-changing routes.

5. **Route file missing authentication: webOptimization.js** [HIGH]
   → Add authentication middleware (e.g., authenticate) to all state-changing routes.

6. **Hardcoded password detected** [CRITICAL]
   → Move to environment variables and reference via process.env.

7. **Possible missing await on Mongoose operation: .find()** [HIGH]
   → Add 'await' before the Mongoose operation or chain .exec().

8. **Possible missing await on Mongoose operation: .find()** [HIGH]
   → Add 'await' before the Mongoose operation or chain .exec().

9. **Possible missing await on Mongoose operation: .findOne()** [HIGH]
   → Add 'await' before the Mongoose operation or chain .exec().

10. **Possible missing await on Mongoose operation: .find()** [HIGH]
   → Add 'await' before the Mongoose operation or chain .exec().

